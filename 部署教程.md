# 部署到 Vercel + 绑定自己的域名

一共三步，做完之后写文章只需要 `git push`。

---

## 第 1 步 · Vercel 导入项目

1. 打开 https://vercel.com ，**用 GitHub 账号登录**
2. Add New → Project → 导入 `yyl1208/youngke-blog`
3. 展开配置面板，填这四项（其余保持默认）：

| 配置项 | 值 |
|---|---|
| Framework Preset | **Other** |
| Build Command | `npm run build` |
| Output Directory | **`out`** |
| Node.js Version | 22 |

> 为什么是 Other 而不是 Next.js：这个项目是 `output: 'export'` 的纯静态导出，产物在 `out/` 不在 `.next/`。选 Next.js 预设 Vercel 会按服务端渲染那套去找产物，部署出来是 404。

4. Environment Variables 加一条：

```
SITE_URL = https://你的域名
```

> 给 RSS 和页面 metadata 拼绝对地址用。不设的话 RSS 里的链接会缺域名。

5. Deploy。一分钟后拿到 `xxx.vercel.app`，先打开确认正常：明暗主题切换、⌘K 搜索、`/rss.xml`。

> `vercel.json` 已经在仓库里，作用是保留结尾斜杠，和构建产物保持一致，不用改。

---

## 第 2 步 · Vercel 里加域名

Settings → Domains，**两个都加**（只加一个会导致根域和 www 互相重定向进死循环）：

- `yourdomain.com`
- `www.yourdomain.com`

加完会显示 DNS 要求，抄下来进第 3 步。**此时 Invalid Configuration 是正常的**，DNS 生效后自动变绿。

---

## 第 3 步 · DNS 加三条记录

在你的 DNS 服务商（阿里云的话：控制台 → 云解析 DNS → 解析设置）加：

| 主机记录 | 类型 | 记录值 |
|---|---|---|
| `@` | **A** | `76.76.21.21` |
| `www` | **CNAME** | `cname-china.vercel-dns.com` |
| `_vercel` | TXT | Vercel 给的 `vc-domain-verify=...`（没要求就不加） |

TTL 填 10 分钟。

两个坑：

- **根域只能 A 记录。** 阿里云免费版不支持根域 CNAME / ALIAS；而且同一主机记录下 A 和 CNAME 会冲突，报 `DomainRecordConflict`。
- **`www` 必须用 `cname-china` 那条。** Vercel 在中国大陆没有节点，`cname.vercel-dns.com` 和 `*.vercel.app` 在国内基本打不开；`cname-china.vercel-dns.com` 是它专门给国内的线路，这一字之差就是能不能访问的区别。

验证：

```bash
dig yourdomain.com A +short          # 应返回 76.76.21.21
dig www.yourdomain.com CNAME +short  # 应返回 cname-china.vercel-dns.com
```

生效后回 Vercel 点 Refresh，两个域名都显示绿色 **Valid Configuration**，证书自动签发。

---

## 验收清单

- [ ] `https://yourdomain.com` 和 `www` 都能打开，18 个页面全部 200
- [ ] HTTPS 证书有效，无安全警告
- [ ] 主题切换正常，刷新不闪白
- [ ] ⌘K 搜索能搜到文章和知识库
- [ ] `/rss.xml` 里的链接域名正确
- [ ] 手机上看导航收成汉堡菜单
- [ ] 推一个小改动到 `main`，一分钟后线上自动更新
- [ ] 用 https://www.itdog.cn/http/ 测国内多地延迟，确认可接受

---

## 常见问题

| 现象 | 原因 | 处理 |
|---|---|---|
| 部署完 404 / 白屏 | Output Directory 不是 `out` | 改成 `out` |
| 两个域名反复跳转 | 根域和 www 只加了一个 | 两个都加到 Domains |
| 证书一直 Provisioning | DNS 未生效 | 等 10 分钟；超时检查记录是否写对 |
| 国内打不开 | 用了 `cname.vercel-dns.com` | 换成 `cname-china.vercel-dns.com` |
| 根域加 CNAME 报冲突 | `@` 下已有 A 记录 | 根域就保持 A 记录 |

---

## 两点提醒

**Vercel 免费版条款禁止商业用途。** 个人博客、技术作品集没问题；以后要放广告或付费内容得升 Pro。流量对个人博客远远够用。

**已备案域名指向境外不属于违规，但**接入商（阿里云）会做备案接入检测，可能标记成「未接入」。如果国内访问实在慢到不能用，到时候再挂一份国内静态托管（钱一会儿那边对比过：腾讯云 EdgeOne Pages 免费、含国内节点、同样 GitHub 触发构建），在此之前不用管。
