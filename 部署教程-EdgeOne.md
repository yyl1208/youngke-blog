# youngke-blog 部署教程：EdgeOne Pages + 自有域名

> 适用前提：**域名已完成 ICP 备案**。整套流程约 15 分钟，成本 0 元。
>
> 部署形态：GitHub 存代码 → EdgeOne 自动构建 → 边缘节点给用户访问。不碰服务器，不手动上传文件。

---

## 为什么是 EdgeOne Pages

| 维度 | 情况 |
|---|---|
| 成本 | 免费套餐覆盖个人博客，无需信用卡 |
| 国内访问 | 有中国大陆边缘节点，前提是域名已备案并选对加速区域 |
| 更新方式 | 连上 GitHub 仓库后，`git push` 自动重新构建，不用手动传文件 |
| HTTPS | 自动签发和续期，不用自己申请证书 |
| 运维 | 无服务器、无 Nginx、无容器 |

个人博客的月流量通常在 **1–3 GB** 量级，免费额度绰绰有余。

---

## 一、前置准备

- [ ] 腾讯云账号，已完成**实名认证**
- [ ] GitHub 账号，能访问 `yyl1208/youngke-blog`
- [ ] 一个**已备案**的域名
- [ ] 代码已推到 GitHub `main` 分支（已经完成）

---

## 二、创建 Pages 项目

1. 登录腾讯云控制台 → 进入 **EdgeOne** → 找到 **Pages / Makers**
2. 点「**创建项目**」，选「**通过 Git 仓库创建**」
3. 授权 GitHub（只是让腾讯云读取仓库，不会改你的代码）
4. 选仓库：`yyl1208/youngke-blog`
5. 选分支：`main`

---

## 三、构建配置（最容易配错的一步）

| 配置项 | 填什么 | 说明 |
|---|---|---|
| **加速区域** | 全球（含中国大陆） | 域名已备案才能选；选了国内访问才快 |
| **框架预设** | Next.js | 若识别出的输出目录是 `.next`，手动改成 `out` |
| **根目录** | `./` | 仓库根目录 |
| **构建命令** | `npm run build` | 与 `package.json` 里的脚本一致 |
| **输出目录** | **`out`** | ⚠️ 必须是 `out`，不是 `.next` |
| **Node.js 版本** | `22` | 至少 18 |
| **环境变量** | `SITE_URL` = `https://你的域名` | 供 RSS 和页面 metadata 生成绝对链接 |

### 两个必须注意的坑

> **① 输出目录一定填 `out`。**
> 项目的 `next.config.ts` 里是 `output: 'export'`，属于纯静态导出，构建产物落在 `out/`。如果按 Next.js 默认填 `.next`，那是服务端运行时代码，部署出来会打不开。

> **② 千万不要设置 `DEPLOY_TARGET` 环境变量。**
> 这个变量只在部署到 GitHub Pages 子路径时才用。一旦设成 `github-pages`，产物里所有资源链接都会带上 `/youngke-blog` 前缀，绑到自己的域名上会**整站 404**。

---

## 四、首次部署与验收

点「开始部署」，观察构建日志。首次会依次经历：拉取仓库 → 安装依赖 → 执行构建 → 上传产物。

成功后平台会分配一个临时域名（形如 `xxxxx.edgeone.app`）。**先用这个临时域名验收**，确认没问题再绑域名，能省掉一次排查。

验收清单：

- [ ] 首页 / 文章 / 知识库 / 经历 / 关于，五个页面都能打开
- [ ] 样式正常 —— 说明 CSS 加载成功，没有路径前缀问题
- [ ] 点进一篇文章，正文排版和代码块高亮正常
- [ ] 按 `⌘K` 能唤起搜索并搜到结果
- [ ] 手机上打开，布局不塌

---

## 五、绑定自有域名

1. 进入项目 → **自定义域名** → 添加域名
2. 填入你要用的地址，例如根域名 `example.com` 或子域名 `blog.example.com`
3. EdgeOne 会给出**需要配置的解析记录**（一般是 CNAME）
4. 回到你的 DNS 服务商，按它给的记录添加

以腾讯云 DNSPod 为例：

| 场景 | 类型 | 主机记录 | 记录值 |
|---|---|---|---|
| 用根域名 `example.com` | CNAME | `@` | EdgeOne 给出的地址 |
| 用子域名 `blog.example.com` | CNAME | `blog` | EdgeOne 给出的地址 |

> **记录值不要自己猜**，EdgeOne 页面显示什么就填什么 —— 不同接入方式给出的地址不同。

---

## 六、等待生效

| 项 | 通常耗时 |
|---|---|
| DNS 解析生效 | 几分钟到几小时 |
| HTTPS 证书签发 | 几分钟，EdgeOne 自动完成 |

---

## 七、最终验收

用你自己的域名访问，重点确认两件事：

1. 地址栏是 **https**，没有「不安全」提示
2. 打开开发者工具 → Network 面板 → 刷新，**所有资源都是 200，没有 404**

有 404 基本就是路径前缀的问题，回到第三步看那两个警告。

---

## 八、以后怎么发文章

本地写完推送即可：

```bash
git add content/
git commit -m "post: 文章标题"
git push
```

EdgeOne 检测到 main 分支有提交后自动重新构建，一两分钟后线上更新。不用做任何别的事。

---

## 常见问题

| 现象 | 原因 | 处理 |
|---|---|---|
| 部署成功但页面空白、样式全丢 | 资源路径前缀不对 | 检查是否误设了 `DEPLOY_TARGET`；确认输出目录是 `out` |
| 构建失败，提示找不到 out 目录 | 构建命令没跑通 | 先看构建日志；本地跑一遍 `npm run build` 确认能过 |
| 安装依赖很慢或超时 | 走的境外 npm 源 | 在项目根目录加一个 `.npmrc`，内容 `registry=https://registry.npmmirror.com` |
| 临时域名能开，自己的域名 404 | CNAME 没生效或主机记录填错 | 用 `dig 你的域名` 检查解析结果 |
| 提示证书错误 / 浏览器拦截 | 证书还在签发，或加速区域选错 | 等几分钟；仍不行检查加速区域是否选了「含中国大陆」 |
| 国内打开很慢 | 加速区域选了「不含中国大陆」 | 改成「全球（含中国大陆）」，前提是域名已备案 |

---

## 附：本次改动涉及的项目约定

代码里为了同时兼容两种部署形态，留了一个开关（`lib/site.ts`）：

| 部署形态 | 环境变量 | `basePath` |
|---|---|---|
| 自定义域名（当前用法） | 不设 | `''` |
| GitHub Pages 子路径 | `DEPLOY_TARGET=github-pages` | `/youngke-blog` |

站点绝对地址由 `SITE_URL` 注入。换域名时只动这两个变量，其余代码不用改。
